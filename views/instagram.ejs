<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style1.css"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    
</head>
<body>
    <div class="profile-container">
        <h1>@<%= data.name %></h1>

        <button class="btn btn-primary btn-custom">Follow</button>
        <button class="btn btn-primary btn-custom">Message</button>

        <div class="stats">
            <span><strong><%= data.followers %></strong> Followers</span> &nbsp;&nbsp; 
            <span><strong><%= data.following %></strong> Following</span>
        </div>

        <hr>

        <div class="post-grid">
            <% for (let post of data.posts) { %>
                <div class="post-item">
                    <img src="<%= post.image %>" alt="Post Image">
                    <p>
                        <i class="fa-regular fa-heart like-btn"></i>
                        <strong><%= post.likes %></strong>
                         &nbsp;&nbsp;&nbsp;&nbsp;
                         <i class="fa-regular fa-comment-dots"></i>
                         <strong><%= post.comments %></strong>
                    </p>
                </div>
            <% } %>
        </div>
    </div>
</body>
<script src="/follow.js"></script>
</html>
